
plate_separation = 70; // 70mm gives a 75mm stage height, which is correct for 45mm parfocal objectives
                       // For 35mm parfocal distance, reduce this to 60mm.
top_mounting_screws_centre = [0, 50, plate_separation]; // where the top mounting screws go
plate_dimensions = [250, 160]; // size of the top plate
plate_support_inset = 20; // mounting holes are inset by this amount
plate_support_r = 10; // radius of the support posts at the corners
plate_t = 5; // thickness of the acrylic

d = 0.05;